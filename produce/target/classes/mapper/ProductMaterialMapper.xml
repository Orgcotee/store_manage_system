<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.produce.mapper.ProductMaterialMapper">
    <select id="selectProductMaterial" resultType="com.example.produce.domain.vo.ProductMaterialVO">
        SELECT
            id,
            name,
            type,
            unit,
            barcode,
            image,
            min_stock,
            create_time,
            update_time
        FROM
            product_material
    </select>
</mapper>