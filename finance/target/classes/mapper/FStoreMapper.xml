<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.example.finance.mapper.FStoreMapper">
    <select id="selectAll" resultType="com.example.finance.domain.entity.FStore">
        SELECT * FROM f_store
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO f_store (id,manager, address, phone)
        VALUES (#{id},#{manager}, #{address}, #{phone})
    </insert>

    <update id="updateById">
        UPDATE f_store SET
                           manager = #{manager},
                           address = #{address},
                           phone = #{phone}
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM f_store WHERE id = #{id}
    </delete>
</mapper>